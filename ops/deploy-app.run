deploy
# ^ mode word on line 1: inspect (read-only discovery) | deploy (write + restart).
# Editing this file and pushing to main runs .github/workflows/deploy-app.yml.
# The paths filter needs a real diff each run, so bump the counter below when re-firing.
# Last deploy: 207f11c -> afbc0d9 (jvto-cms-bootstrap) resolvePage asset hydration verified live 2026-07-30.
# run-counter: 7
