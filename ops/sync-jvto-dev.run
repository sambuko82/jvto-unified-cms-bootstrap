plan
# apply #3 SUCCEEDED 2026-07-30 (run 30519490534): +74 Drive destination photos + hero/gallery
# sections synced into jvto_dev — additive, nothing deleted. rollback.sql = run artifact 8750238071.
# (Attempts 1-2 hit a transient connect ETIMEDOUT to :5432 from GitHub runners; attempt 3 on a
# fresh runner IP connected and committed. Read-only plan had reached the DB fine.)
# Default is `plan` (dry-run, writes nothing). Set line 1 to `apply` + bump this comment to
# write jvto_dev again.
